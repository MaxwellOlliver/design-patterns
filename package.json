{
  "name": "design-system",
  "private": true,
  "version": "1.0.0",
  "description": "Design patterns implementation workspace",
  "scripts": {
    "build:all": "pnpm --recursive build",
    "dev:all": "pnpm --recursive dev",
    "install:all": "pnpm install --recursive"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@10.11.0"
}
